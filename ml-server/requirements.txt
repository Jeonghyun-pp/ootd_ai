# FastAPI ML 서버
# NOTE: torch is installed separately in Dockerfile (CPU-only) — do NOT add here
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
numpy>=1.24.0
python-multipart>=0.0.6
timm>=0.9.0
Pillow>=10.0.0
